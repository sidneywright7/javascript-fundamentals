typeof inputs: undefined, null, boolean, number, bigint, string, symbol, symbol, function, and object 
JavaScript has data types, which are categorized into two groups: primitive (representing single, immutable values) and non-primitive or object (representing complex data structures). 
primitive: string, number, boolean, undefined, null, symbol, bigint
non-primitivd (object): object, array, function